914-929

---ANALYSIS---
Number of images: 2

CSS rules affecting image size from <style> section (lines 7-597):

1. .slide (lines 28-39):
   - display: flex
   - align-items: center
   - justify-content: center
   - width: 100%
   - height: 100%
   (Affects overall slide layout and centering)

2. .slide.active (lines 41-44):
   - opacity: 1
   - z-index: 1
   (Affects visibility when slide is active)

3. .slide-content (lines 58-64):
   - padding: 60px
   - max-width: 1200px
   (Affects content container sizing and spacing)

4. .slide h2 (lines 81-87):
   - font-size: 2.5rem
   - margin-bottom: 20px
   (Affects heading presentation)

5. .slide p (lines 90-97):
   - font-size: 1.8rem
   - margin-bottom: 15px
   (Affects paragraph text sizing)

6. .anim-slide-left (lines 226-228):
   - animation: slideInLeft 1.5s ease-out both
   (Applied to left image container - animation only)

7. .anim-slide-right (lines 230-232):
   - animation: slideInRight 1.5s ease-out both
   (Applied to right image container - animation only)

8. @keyframes slideInLeft (lines 422-431):
   - Defines animation with transform: translateX(-100px) rotate(-5deg) → translateX(0) rotate(-1deg)
   (Animation on left image)

9. @keyframes slideInRight (lines 433-442):
   - Defines animation with transform: translateX(100px) rotate(5deg) → translateX(0) rotate(2deg)
   (Animation on right image)

Note: Actual image sizing on SLAJD 16 is controlled by INLINE STYLES:
- width: 100%
- border: 10px solid #fff
- box-shadow: 0 10px 40px rgba(0,0,0,0.5)
- transform: rotate(-2deg) or rotate(2deg)

Container divs use INLINE STYLES:
- flex: 1
- max-width: 900px
- Parent div: display: flex; gap: 40px; justify-content: center; align-items: center;
---END---
