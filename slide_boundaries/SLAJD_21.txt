1020-1046

---ANALYSIS---
Number of images: 1

CSS rules affecting image size (from <style> section):

1. * (lines 8-12)
   - box-sizing: border-box;
   [Affects how the border property is included in width/height calculations]

2. .slide (lines 28-39)
   - width: 100%;
   - height: 100%;
   [Sets the base slide container to fullscreen, constrains image parent]

3. .slide-content (lines 58-64)
   - padding: 60px;
   - max-width: 1200px;
   [Constrains the content area width, indirectly affects image layout]

4. .slide.active .anim-slide-left (lines 226-228)
   - animation: slideInLeft 1.5s ease-out both;
   [Applies animation to the element containing the image, uses slideInLeft keyframes]

Related keyframe definitions that transform the image container:
- @keyframes slideInLeft (lines 161-170 or 422-431)
  - transform: translateX(-100px) rotate(-5deg) → translateX(0) rotate(-1deg)

Note: The actual image sizing (width: 100%) is controlled by inline styles on the <img> tag itself, not by CSS class selectors in the <style> section. The parent container sizing (flex: 1; max-width: 750px;) is also inline-styled.

---END---
