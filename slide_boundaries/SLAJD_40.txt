1422-1449

---ANALYSIS---
Number of images: 2

CSS rules affecting image size:

FROM INLINE STYLES (HTML):
1. img { width: 100%; height: 100%; object-fit: cover; }
   - Applied to both images in the slide

2. img[0] filter: sepia(0.3) contrast(1.1);
   - Applied only to first image (Klasa Vb rok 1971)

3. Parent container .anim-slide-left, .anim-slide-right-delay { height: 500px; overflow: hidden; }
   - Sets explicit height constraint for image containers

4. Grid container { display: grid; grid-template-columns: 1fr 1fr; gap: 3px; }
   - Defines 2-column equal-width layout for image placement

FROM CSS STYLESHEET (lines 7-597):
1. .slide-content { max-width: 1200px; padding: 60px; }
   - Constrains overall slide content width

2. .slide { display: flex; align-items: center; justify-content: center; padding: 0; }
   - Centers slide content, flex container for alignment

3. .notebook-bg { background-image: url('./assets/zeszyt.png'); background-size: cover; }
   - Applied to slide background (not directly affecting img sizing)

4. .slide.active .anim-slide-left { animation: slideInLeft 1.5s ease-out both; }
   - Applies slideInLeft animation (uses transform: translateX/rotate, not sizing)

5. .slide.active .anim-slide-right-delay { animation: slideInRight 1.5s ease-out 0.5s both; }
   - Applies slideInRight animation with 0.5s delay (uses transform, not sizing)

EFFECTIVE IMAGE SIZING RULES:
- Container height: 500px (hard constraint)
- Image dimensions: 100% width × 100% height (fills container)
- Object-fit: cover (scales and crops to fill container)
- Grid layout: 2 equal columns (1fr 1fr) → each image gets ~50% viewport width
- Overall max-width: 1200px on slide-content

---END---
